// import { onWindowChange } from './helpers'

/**
 * 100vh fix for android 4.1.2 combined with reaction on window change
 */
/* export const fullVhFix = () => {
  const viewportHeight = Math.max(
    document.documentElement.clientHeight,
    window.innerHeight || 0
  )
  document.querySelector('.header').style.height = `${viewportHeight}px`
  document.querySelector('.header__buttons').style.marginTop =
    0.02 * viewportHeight
} */

