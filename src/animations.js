
import { qs, animate, timing, timeFlow } from "./helpers"

/* export const animations = () => {
    
        qs('.first__container').addEventListener('click', () => {
            let elWidth = qs('.first__container').offsetWidth
            animate((progress) => {
                qs('.first__container').style.width = `${elWidth + progress * 300}px`
            })
        })
    
} */